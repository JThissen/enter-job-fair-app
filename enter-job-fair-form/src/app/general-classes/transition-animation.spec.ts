import { TransitionAnimation } from './transition-animation';

describe('TransitionAnimation', () => {
  it('should create an instance', () => {
    expect(new TransitionAnimation()).toBeTruthy();
  });
});
